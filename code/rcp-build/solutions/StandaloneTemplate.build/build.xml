<project name="SomeRCPBuild" default="build">
  <!-- Generated by MPS -->
  <property name="build.dir" location="build" />
  <property name="build.tmp" location="${build.dir}/tmp/SomeRCPBuild" />
  <property name="build.layout" location="${build.dir}/artifacts/SomeRCPBuild" />
  
  <target name="assemble">
    <mkdir dir="${build.layout}" />
  </target>
  
  <target name="build" depends="assemble" />
  
  <target name="clean">
    <delete dir="${build.tmp}" />
    <delete dir="${build.layout}" />
  </target>
</project>