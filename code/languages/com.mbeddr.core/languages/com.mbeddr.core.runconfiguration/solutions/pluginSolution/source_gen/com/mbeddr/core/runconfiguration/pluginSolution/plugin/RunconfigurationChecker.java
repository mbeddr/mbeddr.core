package com.mbeddr.core.runconfiguration.pluginSolution.plugin;

/*Generated by MPS */

import jetbrains.mps.execution.lib.NodeByConcept_Configuration;

public class RunconfigurationChecker {


  public static String check(NodeByConcept_Configuration binary, String launchTimeout, String commandTimeout) {
    if (launchTimeout == null || (launchTimeout == null || launchTimeout.length() == 0)) {
      return "Launch timeout must be set";
    } else {
      try {
        Integer.parseInt(launchTimeout);
      } catch (Exception e) {
        return "Launch timeout must be set to a value greater zero";
      }
    }
    if (binary == null) {
      return "Binary must be selected!";
    }
    if (commandTimeout == null || (commandTimeout == null || commandTimeout.length() == 0)) {
      return "Command timeout must be set";
    } else {
      try {
        Integer.parseInt(commandTimeout);
      } catch (Exception e) {
        return "Command timeout must be set to a value greater zero";
      }
    }
    return null;
  }
}
