package com.mbeddr.core.legacy.pluginSolution.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import jetbrains.mps.refactoring.runtime.access.RefactoringAccess;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import jetbrains.mps.project.MPSProject;
import jetbrains.mps.refactoring.framework.RefactoringContext;
import java.util.Arrays;
import jetbrains.mps.ide.platform.refactoring.StringChooserDialog;
import java.util.List;
import java.util.ArrayList;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import org.jetbrains.mps.openapi.language.SConcept;
import jetbrains.mps.smodel.adapter.structure.MetaAdapterFactory;

public class convertToOpaqueExprWithTypeForRefAction_Action extends BaseAction {
  private static final Icon ICON = null;

  public convertToOpaqueExprWithTypeForRefAction_Action() {
    super("Specifiy type of Referenced Constant", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setExecuteOutsideCommand(true);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    return RefactoringAccess.getInstance(event.getData(MPSCommonDataKeys.MPS_PROJECT)).isApplicable("com.mbeddr.core.legacy.refactorings.convertToOpaqueExprWithTypeForRef", event.getData(MPSCommonDataKeys.NODE));
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      SNode node = event.getData(MPSCommonDataKeys.NODE);
      if (node != null && !(SNodeOperations.isInstanceOf(node, CONCEPTS.GlobalConstantRef$ML))) {
        node = null;
      }
      if (node == null) {
        return false;
      }
    }
    {
      MPSProject p = event.getData(MPSCommonDataKeys.MPS_PROJECT);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    final String newType;
    newType = convertToOpaqueExprWithTypeForRefAction_Action.this.getInputFornewType(event);
    if (newType == null) {
      return;
    }

    event.getData(MPSCommonDataKeys.MPS_PROJECT).getRepository().getModelAccess().runReadInEDT(new Runnable() {
      public void run() {
        RefactoringAccess.getInstance(event.getData(MPSCommonDataKeys.MPS_PROJECT)).getRefactoringFacade().execute(RefactoringContext.createRefactoringContextByName("com.mbeddr.core.legacy.refactorings.convertToOpaqueExprWithTypeForRef", Arrays.asList("newType"), Arrays.asList(newType), event.getData(MPSCommonDataKeys.NODE), event.getData(MPSCommonDataKeys.MPS_PROJECT)));
      }
    });
  }
  private String getInputFornewType(final AnActionEvent event) {
    return StringChooserDialog.getString(event.getData(MPSCommonDataKeys.MPS_PROJECT).getProject(), "Enter Type Alias", "Enter Type Alias", "");
  }
  private List<SNode> getInputAsList(final AnActionEvent event) {
    {
      List<SNode> result = new ArrayList<SNode>();
      ListSequence.fromList(result).addElement(event.getData(MPSCommonDataKeys.NODE));
      return result;
    }
  }

  private static final class CONCEPTS {
    /*package*/ static final SConcept GlobalConstantRef$ML = MetaAdapterFactory.getConcept(0x6d11763d483d4b2bL, 0x8efc09336c1b0001L, 0x34953142306a769bL, "com.mbeddr.core.modules.structure.GlobalConstantRef");
  }
}
