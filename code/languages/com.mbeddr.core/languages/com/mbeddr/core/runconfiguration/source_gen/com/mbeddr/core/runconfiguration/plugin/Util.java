package com.mbeddr.core.runconfiguration.plugin;

/*Generated by MPS */

import jetbrains.mps.vfs.IFile;
import jetbrains.mps.smodel.SNode;
import jetbrains.mps.baseLanguage.closures.runtime.Wrappers;
import jetbrains.mps.project.IModule;
import jetbrains.mps.smodel.ModelAccess;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import jetbrains.mps.generator.traceInfo.TraceInfoUtil;
import jetbrains.mps.vfs.FileSystem;

public class Util {
  public Util() {
  }

  public static IFile getPath(final SNode binary) {
    final Wrappers._T<IModule> module = new Wrappers._T<IModule>();
    final Wrappers._T<String> packageName = new Wrappers._T<String>();
    final Wrappers._T<String> sourceName = new Wrappers._T<String>();
    ModelAccess.instance().runReadAction(new Runnable() {
      public void run() {
        module.value = SNodeOperations.getModel(binary).getModelDescriptor().getModule();
        packageName.value = SNodeOperations.getModel(binary).getLongName().replace(".", "/");
        sourceName.value = TraceInfoUtil.getUnitName(SNodeOperations.getParent(binary));
      }
    });
    return FileSystem.getInstance().getFileByPath(module.value.getGeneratorOutputPath()).getDescendant(packageName.value).getDescendant(sourceName.value);
  }
}
