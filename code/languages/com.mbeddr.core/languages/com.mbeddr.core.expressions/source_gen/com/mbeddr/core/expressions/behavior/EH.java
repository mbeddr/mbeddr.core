package com.mbeddr.core.expressions.behavior;

/*Generated by MPS */

import java.math.BigInteger;
import java.math.BigDecimal;

public abstract class EH {
  public static boolean isBoolean(Object o) {
    return o instanceof Boolean;
  }
  public static boolean isTrue(Object o) {
    return isBoolean(o) && ((Boolean) o).booleanValue() == true;
  }
  public static boolean isFalse(Object o) {
    return !(isTrue(o));
  }
  public static boolean isNumber(Object o) {
    return o instanceof Number;
  }
  public static boolean isNaturalNumber(Object o) {
    return o instanceof BigInteger;
  }
  public static boolean isFloatingPointNumber(Object o) {
    return o instanceof BigDecimal;
  }

  public static boolean isZero(Object o) {
    if (isNaturalNumber(o)) {
      BigInteger n = ((BigInteger) o);
      return BigInteger.ZERO.compareTo(n) == 0;
    }
    if (isFloatingPointNumber(o)) {
      BigDecimal n = ((BigDecimal) o);
      return BigDecimal.ZERO.compareTo(n) == 0;

    }
    return false;
  }
  public static BigInteger asInt(Object o) {
    if (o instanceof BigInteger) {
      return ((BigInteger) o);
    }
    throw new IllegalArgumentException(o.getClass().getName() + " is not a bigInt");
  }
  public static BigDecimal asDec(Object o) {
    if (isNaturalNumber(o)) {
      return new BigDecimal(asInt(o));
    }
    if (isFloatingPointNumber(o)) {
      return ((BigDecimal) o);
    }
    throw new IllegalArgumentException(o.getClass().getName() + " is not a number");
  }

  public static Number asNumber(Object o) {
    if (o == null) {
      throw new IllegalArgumentException("not a number - was null");
    }
    if (isNumber(o)) {
      return ((Number) o);
    }
    throw new IllegalArgumentException(o.getClass().getName() + " is not a number");
  }
}
