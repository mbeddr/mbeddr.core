* How do we deal with all the system-level header files? Right now ArbitraryText

* Copying in accessory models and rebinding must be simplified.
  to avoid the manually scripted rebinding crap, I have not renamed the copied-in module. 
  Seems to work. So far.

* Error handling in case of the queue



TODO:
* use an external module as a proxy for the system headers
* Sascha's stuff
* Dass man queues als shared markieren muss ist doof.
Move the locking and blocking into the enqueue dequeue functions
  (if queue is blockable)  
* fix the timing
* task callable functions
  proxies/wiring for resoures
* prio-based scheduling
* Amalthea Export
* Analyses?
* Comp





