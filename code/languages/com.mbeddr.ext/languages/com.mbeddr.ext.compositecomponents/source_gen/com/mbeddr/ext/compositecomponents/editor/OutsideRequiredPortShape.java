package com.mbeddr.ext.compositecomponents.editor;

/*Generated by MPS */

import de.itemis.mps.editor.diagram.runtime.shape.AbstractShape;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import de.itemis.mps.editor.diagram.runtime.shape.IShapeStyle;
import java.awt.Polygon;
import java.awt.BasicStroke;
import com.mbeddr.ext.components.editor.DiagramColors;

public class OutsideRequiredPortShape extends AbstractShape {


  public OutsideRequiredPortShape() {
  }




  @Override
  protected void draw(final Graphics2D graphics, final Rectangle2D bounds, IShapeStyle style) {
    int x = ((int) bounds.getX());
    int y = ((int) bounds.getY());
    int h = ((int) bounds.getHeight());
    int w = ((int) bounds.getWidth());


    int d = h / 3;
    int[] xs = {x + d, x + w, x + w, x + d, x, x, x + d};
    int[] ys = {y, y, y + h, y + h, y + h - d, y + d, y};
    Polygon p = new Polygon();
    p.addPoint(x, y);
    p.addPoint(x + w, y);
    p.addPoint(x + w + d, y + d);

    p.addPoint(x + w + d, y + (h / 2) - 1);
    p.addPoint(x + w + d, y + (h / 2) + 1);

    p.addPoint(x + w + d, y + h - d);
    p.addPoint(x + w, y + h);
    p.addPoint(x, y + h);
    p.addPoint(x, y);





    graphics.setStroke(new BasicStroke(1));
    graphics.setColor(DiagramColors.COMPONENT_BACKGROUND);
    graphics.fill(bounds);
    graphics.setColor(DiagramColors.COMPONENT_BORDER);
    graphics.draw(bounds);
  }

}
