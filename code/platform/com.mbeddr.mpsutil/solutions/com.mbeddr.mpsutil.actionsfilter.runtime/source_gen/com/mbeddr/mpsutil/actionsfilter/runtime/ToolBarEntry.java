package com.mbeddr.mpsutil.actionsfilter.runtime;

/*Generated by MPS */

import jetbrains.mps.logging.Logger;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;
import java.io.File;
import jetbrains.mps.util.PathManager;

public class ToolBarEntry {
  private static final Logger LOG = Logger.getLogger(ToolBarEntry.class);

  public enum EntryType {
    TOOLBAR_ACTION(),
    TOOLBAR_GROUP_SEPARATOR()
  }

  public ToolBarEntry() {
  }

  public ToolBarEntry(@NotNull EntryType type) {
    this.entryType = type;
  }

  public ToolBarEntry(String actionId, String icon, @NotNull EntryType type) {
    this.actionId = actionId;
    this.icon = icon;
    this.entryType = type;
  }

  @NotNull
  public EntryType entryType;
  @Nullable
  public String actionId;
  @Nullable
  public String icon;

  public EntryType getEntryType() {
    return entryType;
  }

  public void setEntryType(EntryType entryType) {
    this.entryType = entryType;
  }

  public String getId() {
    return actionId;
  }

  public String getIcon() {
    return icon;
  }

  public void setId(String id) {
    this.actionId = id;
  }

  public void setIcon(String icon) {
    this.icon = icon;
  }

  public String getIconPath() {
    if ((icon == null || icon.length() == 0)) {
      return null;
    }
    File iconFile = new File(PathManager.getHomePath() + File.separator + icon);
    if (!(iconFile.exists())) {
      if (LOG.isWarningLevel()) {
        LOG.warning(String.format("Could not find icon [%s] for action [%s]", icon, actionId));
      }
      return null;
    }
    return iconFile.getAbsolutePath();
  }
}
