package com.mbeddr.mpsutil.plantuml.pluginSolution.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.icons.AllIcons;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import com.intellij.openapi.project.Project;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import java.awt.Frame;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;
import org.jetbrains.annotations.NotNull;
import java.awt.FileDialog;
import java.io.FilenameFilter;
import java.io.File;
import jetbrains.mps.plugins.projectplugins.ProjectPluginManager;

public class Save_Action extends BaseAction {
  private static final Icon ICON = AllIcons.Actions.MenuSaveall;

  public Save_Action() {
    super("Save as *.svg", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      Project p = event.getData(CommonDataKeys.PROJECT);
      if (p == null) {
        return false;
      }
    }
    {
      Frame p = event.getData(MPSCommonDataKeys.FRAME);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    FileDialog chooser = new FileDialog(event.getData(MPSCommonDataKeys.FRAME), "Save as *.svg image", FileDialog.SAVE);
    chooser.setFilenameFilter(new FilenameFilter() {
      public boolean accept(File file, String name) {
        return name.toUpperCase().endsWith(".SVG");
      }
    });

    chooser.setVisible(true);
    String directory = chooser.getDirectory();
    String file = chooser.getFile();
    if (directory != null && file != null) {
      SVGViewer_Tool content = ProjectPluginManager.getInstance(event.getData(CommonDataKeys.PROJECT)).getTool(SVGViewer_Tool.class);
      content.saveTo(new File(directory + file));

    }


  }
}
