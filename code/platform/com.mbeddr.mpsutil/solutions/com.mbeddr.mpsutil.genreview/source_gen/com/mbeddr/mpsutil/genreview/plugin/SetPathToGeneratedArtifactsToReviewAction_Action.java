package com.mbeddr.mpsutil.genreview.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.icons.AllIcons;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import java.io.File;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.mps.openapi.model.SModel;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;
import com.intellij.openapi.project.Project;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import jetbrains.mps.plugins.projectplugins.ProjectPluginManager;
import jetbrains.mps.vfs.IFile;
import jetbrains.mps.smodel.SModelOperations;

public class SetPathToGeneratedArtifactsToReviewAction_Action extends BaseAction {
  private static final Icon ICON = AllIcons.General.Settings;

  public SetPathToGeneratedArtifactsToReviewAction_Action() {
    super("Set Path to the Generated Artifacts to Be Reviewed", "The location to the generated artifacts is set as the generation output for this model", ICON);
    this.setIsAlwaysVisible(true);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    File file = SetPathToGeneratedArtifactsToReviewAction_Action.this.getOutputPath(event);
    return file != null && file.exists();
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      SModel model = event.getData(MPSCommonDataKeys.CONTEXT_MODEL);
      if (model == null) {
        return false;
      }
    }
    {
      Project p = event.getData(CommonDataKeys.PROJECT);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    CodeReviewTool_Tool crt = ProjectPluginManager.getInstance(event.getData(CommonDataKeys.PROJECT)).getTool(CodeReviewTool_Tool.class);
    crt.setModelFolder(SetPathToGeneratedArtifactsToReviewAction_Action.this.getOutputPath(event));
  }
  private File getOutputPath(final AnActionEvent event) {
    IFile outputLocation = SModelOperations.getOutputLocation(event.getData(MPSCommonDataKeys.CONTEXT_MODEL));
    if (outputLocation == null) {
      return null;
    }
    return new File(outputLocation.getPath());
  }
}
