package com.mbeddr.mpsutil.genreview.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.icons.AllIcons;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import com.intellij.openapi.project.Project;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import jetbrains.mps.project.MPSProject;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.plugins.projectplugins.ProjectPluginManager;
import com.intellij.openapi.application.ApplicationManager;

public class CodeReviewToolOpenerAction_Action extends BaseAction {
  private static final Icon ICON = AllIcons.Actions.GeneratedFolder;

  public CodeReviewToolOpenerAction_Action() {
    super("Open Generated Artifacts Review Tool", "Opens a tool for reviewing the generated artifacts", ICON);
    this.setIsAlwaysVisible(true);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      Project p = event.getData(CommonDataKeys.PROJECT);
      if (p == null) {
        return false;
      }
    }
    {
      MPSProject p = event.getData(MPSCommonDataKeys.MPS_PROJECT);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    final CodeReviewTool_Tool crt = ProjectPluginManager.getInstance(event.getData(CommonDataKeys.PROJECT)).getTool(CodeReviewTool_Tool.class);
    crt.setRepository(event.getData(MPSCommonDataKeys.MPS_PROJECT).getRepository());
    ApplicationManager.getApplication().invokeLater(() -> crt.openTool(true));
  }
}
