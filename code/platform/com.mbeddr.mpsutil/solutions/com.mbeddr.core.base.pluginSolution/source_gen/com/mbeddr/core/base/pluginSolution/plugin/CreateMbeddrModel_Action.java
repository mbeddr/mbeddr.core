package com.mbeddr.core.base.pluginSolution.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import org.jetbrains.annotations.NotNull;
import com.intellij.openapi.project.Project;
import jetbrains.mps.project.MPSProject;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;
import org.jetbrains.mps.openapi.module.SModule;
import jetbrains.mps.baseLanguage.closures.runtime.Wrappers;
import jetbrains.mps.project.AbstractModule;
import org.jetbrains.mps.openapi.model.SModel;
import jetbrains.mps.ide.projectPane.ProjectPane;

public class CreateMbeddrModel_Action extends BaseAction {
  private static final Icon ICON = null;

  public CreateMbeddrModel_Action() {
    super("New Model", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setExecuteOutsideCommand(true);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    if (!(MbeddrMenuHelper.isMbeddrMenuEnabled(event.getData(CommonDataKeys.PROJECT))) || !(MbeddrMenuHelper.isMPSorMbeddrStandalone())) {
      event.getPresentation().setVisible(false);
      event.getPresentation().setEnabled(false);
      return false;
    }

    event.getPresentation().setVisible(true);
    event.getPresentation().setEnabled(true);
    return true;
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      Project p = event.getData(CommonDataKeys.PROJECT);
      if (p == null) {
        return false;
      }
    }
    {
      MPSProject p = event.getData(MPSCommonDataKeys.MPS_PROJECT);
      if (p == null) {
        return false;
      }
    }
    {
      SModule p = event.getData(MPSCommonDataKeys.CONTEXT_MODULE);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {

    final Wrappers._T<NewMbeddrModelDialog> dialog = new Wrappers._T<NewMbeddrModelDialog>();
    final String namespace = event.getData(MPSCommonDataKeys.CONTEXT_MODULE).getModuleName();
    event.getData(MPSCommonDataKeys.MPS_PROJECT).getRepository().getModelAccess().runReadAction(new Runnable() {
      public void run() {
        AbstractModule m = ((AbstractModule) event.getData(MPSCommonDataKeys.CONTEXT_MODULE));
        String stereotype = "";
        dialog.value = new NewMbeddrModelDialog(event.getData(MPSCommonDataKeys.MPS_PROJECT), m, namespace, stereotype, new MbeddrDevKitFilter());
      }
    });
    dialog.value.show();
    SModel model = dialog.value.getResult();

    if (model != null) {
      ProjectPane.getInstance(event.getData(MPSCommonDataKeys.MPS_PROJECT)).selectModel(model, false);
    }
  }
}
