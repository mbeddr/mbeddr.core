package com.mbeddr.core.base.pluginSolution.plugin;

/*Generated by MPS */

import javax.swing.JPanel;
import javax.swing.JCheckBox;
import com.intellij.uiDesigner.core.GridLayoutManager;
import com.intellij.uiDesigner.core.GridConstraints;
import javax.swing.JLabel;

public class MbeddrPreferencePanel extends JPanel {
  private JCheckBox myMenuEnabledBox = new JCheckBox();
  public MbeddrPreferencePanel() {
    super(new GridLayoutManager(2, 2));
    init();
  }

  private void init() {
    GridConstraints constrain = new GridConstraints(0, 0, 1, 1, GridConstraints.ANCHOR_NORTHWEST, GridConstraints.FILL_HORIZONTAL, GridConstraints.SIZEPOLICY_CAN_SHRINK | GridConstraints.SIZEPOLICY_CAN_GROW, GridConstraints.SIZEPOLICY_FIXED, null, null, null);

    this.add(new JLabel("Enable Mbeddr menu extensions"), constrain);
    constrain.setColumn(constrain.getColumn() + 1);
    this.add(myMenuEnabledBox, constrain);
  }

  public void setMenuEnabled(boolean enabled) {
    myMenuEnabledBox.setSelected(enabled);
  }
  public boolean getMenuEnabled() {
    return myMenuEnabledBox.isSelected();
  }
}
