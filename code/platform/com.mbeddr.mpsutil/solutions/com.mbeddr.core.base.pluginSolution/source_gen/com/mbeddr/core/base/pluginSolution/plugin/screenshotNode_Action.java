package com.mbeddr.core.base.pluginSolution.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.icons.AllIcons;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import jetbrains.mps.nodeEditor.EditorComponent;
import jetbrains.mps.ide.editor.MPSEditorDataKeys;
import com.intellij.openapi.project.Project;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.nodeEditor.selection.EditorCellLabelSelection;
import jetbrains.mps.openapi.editor.cells.EditorCell;
import java.io.File;
import java.awt.datatransfer.StringSelection;
import java.awt.Toolkit;
import java.io.IOException;
import com.intellij.openapi.application.ApplicationManager;
import com.intellij.openapi.ui.Messages;

public class screenshotNode_Action extends BaseAction {
  private static final Icon ICON = AllIcons.General.Print;

  public screenshotNode_Action() {
    super("Print Editor", "", ICON);
    this.setIsAlwaysVisible(true);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
    updateInBackground(true);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      EditorComponent editorComponent = event.getData(MPSEditorDataKeys.EDITOR_COMPONENT);
      if (editorComponent != null && editorComponent.isInvalid()) {
        editorComponent = null;
      }
      if (editorComponent == null) {
        return false;
      }
    }
    {
      Project p = event.getData(CommonDataKeys.PROJECT);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    EditorCellLabelSelection selection = as_f1xjau_a0a0a5(event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getSelectionManager().getSelection(), EditorCellLabelSelection.class);

    EditorCell cell = (selection == null || selection.getSelectionStart() != selection.getSelectionEnd() ? event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getSelectedCell() : event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getRootCell());

    String path = event.getData(CommonDataKeys.PROJECT).getBasePath() + "/cell-screenshots/";
    new File(path).mkdirs();
    String filename = path + "screenshot-node-" + cell.getSNode().getNodeId().toString() + ".png";
    try {
      CellEditorScreenshooter.writeImage(cell, filename);
      StringSelection sl = new StringSelection(filename);
      Toolkit.getDefaultToolkit().getSystemClipboard().setContents(sl, null);
    } catch (final IOException ex) {
      ApplicationManager.getApplication().invokeLater(() -> Messages.showErrorDialog(event.getData(CommonDataKeys.PROJECT), "Failed to safe image: " + ex.getMessage(), "Failed to safe image"));
    }
  }
  private static <T> T as_f1xjau_a0a0a5(Object o, Class<T> type) {
    return (type.isInstance(o) ? (T) o : null);
  }
}
