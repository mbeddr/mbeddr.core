package com.mbeddr.core.base.pluginSolution.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import com.intellij.icons.AllIcons;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import jetbrains.mps.plugins.projectplugins.ProjectPluginManager;
import com.intellij.openapi.actionSystem.CommonDataKeys;
import com.intellij.openapi.actionSystem.PlatformDataKeys;
import org.jetbrains.annotations.NotNull;
import com.intellij.openapi.project.Project;
import com.intellij.openapi.wm.ToolWindow;
import java.awt.Frame;
import jetbrains.mps.ide.actions.MPSCommonDataKeys;

public class RefreshF5_Action extends BaseAction {
  private static final Icon ICON = AllIcons.Actions.Refresh;

  public RefreshF5_Action() {
    super("Refresh Tree View", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
    this.setMnemonic("R".charAt(0));
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    if (ProjectPluginManager.getInstance(event.getData(CommonDataKeys.PROJECT)).getTool(GenericTreeTool_Tool.class) == null) {
      return false;
    }
    return event.getData(PlatformDataKeys.TOOL_WINDOW) == ProjectPluginManager.getInstance(event.getData(CommonDataKeys.PROJECT)).getTool(GenericTreeTool_Tool.class).getToolWindow();
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      Project p = event.getData(CommonDataKeys.PROJECT);
      if (p == null) {
        return false;
      }
    }
    {
      ToolWindow p = event.getData(PlatformDataKeys.TOOL_WINDOW);
      if (p == null) {
        return false;
      }
    }
    {
      Frame p = event.getData(MPSCommonDataKeys.FRAME);
      if (p == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    ProjectPluginManager.getInstance(event.getData(CommonDataKeys.PROJECT)).getTool(GenericTreeTool_Tool.class).refresh();
  }
}
