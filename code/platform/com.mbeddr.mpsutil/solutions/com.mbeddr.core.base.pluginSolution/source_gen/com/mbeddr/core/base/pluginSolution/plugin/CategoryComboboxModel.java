package com.mbeddr.core.base.pluginSolution.plugin;

/*Generated by MPS */

import javax.swing.ComboBoxModel;
import java.util.List;
import javax.swing.event.ListDataListener;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;

public class CategoryComboboxModel implements ComboBoxModel {

  private TreeHistory history;
  private String selected;
  private List<ListDataListener> listeners = ListSequence.fromList(new ArrayList<ListDataListener>());

  public CategoryComboboxModel(TreeHistory history) {
    this.history = history;
  }

  public void addListDataListener(ListDataListener listener) {
    ListSequence.fromList(listeners).addElement(listener);
  }

  public Object getElementAt(int i) {
    TreeHistory.HistoryElement curr = history.getCurrent();
    if (curr != null && curr.getTreeNode() != null) {
      return curr.getTreeNode().getAllCategories()[i];
    }
    return null;
  }

  public Object getSelectedItem() {
    return selected;
  }

  public int getSize() {
    TreeHistory.HistoryElement curr = history.getCurrent();
    if (curr != null && curr.getTreeNode() != null) {
      return curr.getTreeNode().getAllCategories().length;
    }
    return 0;
  }

  public void removeListDataListener(ListDataListener listener) {
    ListSequence.fromList(listeners).removeElement(listener);
  }
  public void setSelectedItem(Object object) {
    selected = ((String) object);
  }


}
