package com.mbeddr.mpsutil.interpreter.rt;

/*Generated by MPS */

import jetbrains.mps.logging.Logger;
import org.jetbrains.mps.openapi.model.SNode;
import com.mbeddr.mpsutil.interpreter.behavior.Interpreter__BehaviorDescriptor;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.smodel.structure.ExtensionPoint;
import org.jetbrains.annotations.Nullable;
import java.util.Objects;

public class InterpreterRegistry {
  private static final Logger LOG = Logger.getLogger(InterpreterRegistry.class);
  public static IInterpreter getInterpreterExecutable(SNode interpreter) {
    return getInterpreterExecutable(Interpreter__BehaviorDescriptor.getFqClassName_id4X7QcQ4uRS$.invoke(interpreter));
  }

  public static IInterpreter getInterpreterExecutable(final String fqClassName) {
    if (LOG.isTraceLevel()) {
      LOG.trace("trying to find IInterpreter " + fqClassName);
    }

    IInterpreter result = Sequence.fromIterable(new ExtensionPoint<IInterpreter>("com.mbeddr.mpsutil.interpreter.InterpreterExtensionPoint").getObjects()).findFirst((it) -> {
      if (LOG.isTraceLevel()) {
        LOG.trace("trying IInterpreter " + it);
      }
      return it.getClass().getCanonicalName().equals(fqClassName);
    });

    if (LOG.isTraceLevel()) {
      LOG.trace("IInterpreter found: " + result);
    }

    return result;
  }

  public static Iterable<IInterpreter> findAllInterpreterExecutables() {
    return findAllInterpreterExecutablesForCategory(null);
  }

  public static Iterable<IInterpreter> findAllInterpreterExecutablesForCategory(@Nullable final String category) {
    return Sequence.fromIterable(new ExtensionPoint<IInterpreter>("com.mbeddr.mpsutil.interpreter.InterpreterExtensionPoint").getObjects()).where((it) -> {
      if (category == null) {
        return true;
      }
      return Objects.equals(it.getCategory(), category);
    });
  }
}
