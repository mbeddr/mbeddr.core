package com.mbeddr.mpsutil.interpreter.rt;

/*Generated by MPS */

import jetbrains.mps.logging.Logger;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.mps.openapi.language.SContainmentLink;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import jetbrains.mps.typechecking.TypecheckingFacade;
import com.mbeddr.mpsutil.interpreter.behavior.IInterpreterWrapperType__BehaviorDescriptor;
import jetbrains.mps.typesystem.inference.TypeChecker;
import org.jetbrains.mps.openapi.language.SInterfaceConcept;
import jetbrains.mps.smodel.adapter.structure.MetaAdapterFactory;

public class TypedChildConstraintImpl implements IConstraint {
  private static final Logger LOG = Logger.getLogger(TypedChildConstraintImpl.class);
  @NotNull
  private final SContainmentLink childLink;
  @NotNull
  private final SNode type;

  public TypedChildConstraintImpl(@NotNull SContainmentLink childLink, @NotNull SNode type) {
    this.childLink = childLink;
    this.type = type;
  }

  @Override
  public boolean matchesConstraint(@NotNull SNode node, @NotNull IContext context) {
    SNode matchingChild = ListSequence.fromList(SNodeOperations.getChildren(node, childLink)).first();
    TypecheckingFacade typechecking = TypecheckingFacade.getFromContext();

    if ((matchingChild == null)) {
      return false;
    }

    if (LOG.isTraceLevel()) {
      LOG.trace("matchingChild: " + SNodeOperations.present(matchingChild) + " (" + SNodeOperations.getPointer(matchingChild) + ")");
    }

    SNode matchingChildType = null;
    if (context instanceof ITypeCachingContext) {
      matchingChildType = ((ITypeCachingContext) context).getNodeTypeCache().get(matchingChild);
      if (LOG.isTraceLevel()) {
        LOG.trace("matchingChildType from cache: " + SNodeOperations.present(matchingChildType));
      }
    }
    if (matchingChildType == null) {
      matchingChildType = typechecking.getTypeOf(matchingChild);
      if (context instanceof ITypeCachingContext) {
        ((ITypeCachingContext) context).getNodeTypeCache().set(matchingChild, matchingChildType);
      }
      if (LOG.isTraceLevel()) {
        LOG.trace("matchingChildType from type system: " + SNodeOperations.present(matchingChildType));
      }
    }

    if (matchingChildType == null) {
      throwUnknownTypeOfChild("Type system returned null type for child", node, matchingChild);
    }

    SNode mappedType = context.getMapTypeProvider().mapType(((SNode) matchingChildType), context);
    if (mappedType == null) {
      throwUnknownTypeOfChild(String.format("Mapping type %s resulted in null", SNodeOperations.present(matchingChildType)), node, matchingChild);
    }

    while (SNodeOperations.isInstanceOf(mappedType, CONCEPTS.IInterpreterWrapperType$KD)) {
      mappedType = IInterpreterWrapperType__BehaviorDescriptor.wrappedType_id6bG6MAFRDvi.invoke(SNodeOperations.cast(mappedType, CONCEPTS.IInterpreterWrapperType$KD));
    }

    if (mappedType == null) {
      throwUnknownTypeOfChild(String.format("Unwrapping type %s resulted in null", SNodeOperations.present(mappedType)), node, matchingChild);
    }

    // TypeChecker allows to check isSubTypeByReplacementRules() which TypecheckingFacade doesn't support
    // hence we still need to use TypeChecker for compatibility 
    TypeChecker typeChecker = TypeChecker.getInstance();
    boolean result = typeChecker.getSubtypingManager().isSubtype(mappedType, type);
    if (!(result)) {
      result = typeChecker.getSubtypingManager().isSubTypeByReplacementRules(mappedType, type, true);
    }

    if (LOG.isTraceLevel()) {
      LOG.trace("trying to match childType: " + SNodeOperations.present(mappedType) + " against typeGuard: " + type + " result: " + result);
    }

    return result;
  }

  private String getDescription() {
    return "typed child constraint (link: " + childLink.getName() + ", type: " + SNodeOperations.present(type) + ")";
  }

  private void throwUnknownTypeOfChild(String reason, SNode node, SNode child) {
    throw new UnknownTypeException(String.format("%s\n  while trying to match %s\n  against\n    node %s (%s),\n    child %s (%s)", reason, getDescription(), SNodeOperations.present(node), SNodeOperations.getPointer(node), SNodeOperations.present(child), SNodeOperations.getPointer(child)));
  }

  private static final class CONCEPTS {
    /*package*/ static final SInterfaceConcept IInterpreterWrapperType$KD = MetaAdapterFactory.getInterfaceConcept(0x47f075a6558e4640L, 0xa6067ce0236c8023L, 0x62ec1b29abde62acL, "com.mbeddr.mpsutil.interpreter.structure.IInterpreterWrapperType");
  }
}
