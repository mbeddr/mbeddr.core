package com.mbeddr.mpsutil.richstring.editor;

/*Generated by MPS */

import jetbrains.mps.editor.runtime.cells.AbstractCellAction;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.openapi.editor.EditorContext;
import jetbrains.mps.openapi.editor.selection.SelectionManager;
import java.util.List;
import jetbrains.mps.openapi.editor.cells.EditorCell;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.openapi.editor.cells.CellTraversalUtil;
import jetbrains.mps.openapi.editor.cells.EditorCell_Label;
import jetbrains.mps.editor.runtime.style.StyleAttributesUtil;
import jetbrains.mps.openapi.editor.cells.CellAction;
import jetbrains.mps.openapi.editor.cells.CellActionType;
import jetbrains.mps.nodeEditor.cellProviders.AbstractCellListHandler;
import java.util.Objects;

public class JumpOverApostrophes {

  /*package*/ static AbstractCellAction createAction_LEFT(final SNode node) {
    return new AbstractCellAction() {
      public void execute(EditorContext editorContext) {
        this.execute_internal(editorContext, node);
      }
      public void execute_internal(EditorContext editorContext, SNode node) {
        SelectionManager selectionManager = editorContext.getSelectionManager();
        List<EditorCell> selectedCells = selectionManager.getSelection().getSelectedCells();
        EditorCell firstSelectedCell = ListSequence.fromList(selectedCells).first();
        EditorCell prevLeaf = CellTraversalUtil.getPrevLeaf(firstSelectedCell);
        EditorCell_Label label = (EditorCell_Label) prevLeaf;
        selectionManager.setSelection(label, label.getText().length());
      }

    };
  }
  /*package*/ static AbstractCellAction createAction_RIGHT(final SNode node) {
    return new AbstractCellAction() {
      public void execute(EditorContext editorContext) {
        this.execute_internal(editorContext, node);
      }
      public void execute_internal(EditorContext editorContext, SNode node) {
        SelectionManager selectionManager = editorContext.getSelectionManager();
        List<EditorCell> selectedCells = selectionManager.getSelection().getSelectedCells();
        EditorCell lastSelectedCell = ListSequence.fromList(selectedCells).last();
        EditorCell nextLeaf = CellTraversalUtil.getNextLeaf(lastSelectedCell);
        jetbrains.mps.nodeEditor.cells.EditorCell_Label label = (jetbrains.mps.nodeEditor.cells.EditorCell_Label) nextLeaf;
        boolean firstPositionAllowed = StyleAttributesUtil.isFirstPositionAllowed(label.getStyle());
        selectionManager.setSelection(label, (firstPositionAllowed ? 0 : label.getText().length()));
      }

    };
  }

  public static void setCellActions(EditorCell editorCell, SNode node, EditorContext context) {
    CellAction originalDelete = editorCell.getAction(CellActionType.DELETE);
    CellAction originalBackspace = editorCell.getAction(CellActionType.BACKSPACE);

    // set actions that were actually defined
    setDefinedCellActions(editorCell, node, context);

    // If we set a DELETE action but no BACKSPACE action,
    // use the DELETE action for BACKSPACE as well.
    CellAction delete = editorCell.getAction(CellActionType.DELETE);
    CellAction backspace = editorCell.getAction(CellActionType.BACKSPACE);
    if (delete != originalDelete && backspace == originalBackspace) {
      editorCell.setAction(CellActionType.BACKSPACE, delete);
    }
    if (delete != originalDelete) {
      editorCell.putUserObject(AbstractCellListHandler.ELEMENT_CELL_DELETE_SET, OB);
    }
    if (backspace != originalBackspace) {
      editorCell.putUserObject(AbstractCellListHandler.ELEMENT_CELL_BACKSPACE_SET, OB);
    }
  }

  private static final Object OB = new Object();

  public static void setDefinedCellActions(EditorCell editorCell, SNode node, EditorContext context) {
    // set cell actions from all imported action maps

    // set cell actions defined directly in this action map
    editorCell.setAction(CellActionType.LEFT, createAction_LEFT(node));
    editorCell.setAction(CellActionType.RIGHT, createAction_RIGHT(node));
  }

  public static void setDefinedCellActionsOfType(EditorCell editorCell, SNode node, EditorContext context, CellActionType actionType) {

    // set cell action(s) of the given type from imported action maps

    // set cell action of the given type defined directly in this action map
    if (Objects.equals(actionType, CellActionType.LEFT)) {
      editorCell.setAction(actionType, createAction_LEFT(node));
    }
    if (Objects.equals(actionType, CellActionType.RIGHT)) {
      editorCell.setAction(actionType, createAction_RIGHT(node));
    }
  }
}
