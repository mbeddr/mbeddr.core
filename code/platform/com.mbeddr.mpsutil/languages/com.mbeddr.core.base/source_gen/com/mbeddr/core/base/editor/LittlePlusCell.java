package com.mbeddr.core.base.editor;

/*Generated by MPS */

import jetbrains.mps.nodeEditor.AbstractCellProvider;
import java.awt.Color;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.nodeEditor.cells.EditorCell;
import jetbrains.mps.openapi.editor.EditorContext;
import jetbrains.mps.nodeEditor.cells.EditorCell_Basic;
import java.awt.Graphics;
import jetbrains.mps.nodeEditor.cells.ParentSettings;
import jetbrains.mps.nodeEditor.cells.EditorCell_Collection;
import jetbrains.mps.openapi.editor.TextBuilder;
import jetbrains.mps.editor.runtime.TextBuilderImpl;

public class LittlePlusCell extends AbstractCellProvider {
  private Color color;
  private int padding;
  public LittlePlusCell(SNode node, Color color, int padding) {
    super(node);
    this.color = color;
    this.padding = padding;
  }
  public EditorCell createEditorCell(EditorContext context) {
    EditorCell_Basic result = new EditorCell_Basic(context, getSNode()) {
      public void paintContent(Graphics g, ParentSettings parentSettings) {
        EditorCell_Collection parent = this.getParent();
        int x = getX();
        int y = parent.getY();
        int height = parent.getHeight();

        int x1 = x;
        int y1 = y + padding;
        int size = height - 2 * padding;

        g.setColor(color);
        g.drawRect(x1, y1, size, size);

        int vx = x + size / 2;
        g.drawLine(vx, y + 3, vx, y + size - 1);
        int vy = y + padding + size / 2;
        g.drawLine(x + 2, vy, x + size - 2, vy);


      }
      public void relayoutImpl() {
        EditorCell_Collection parent = this.getParent();
        this.myWidth = 10;
        this.myHeight = 10;
      }
      @Override
      public boolean isSelectable() {
        return false;
      }

      @Override
      public TextBuilder renderText() {
        return new TextBuilderImpl("+");
      }
    };

    return result;
  }
}
