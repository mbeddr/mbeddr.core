package com.mbeddr.core.base.editor;

/*Generated by MPS */

import jetbrains.mps.nodeEditor.AbstractCellProvider;
import java.awt.Color;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.nodeEditor.cells.EditorCell;
import jetbrains.mps.openapi.editor.EditorContext;
import jetbrains.mps.nodeEditor.cells.EditorCell_Basic;
import java.awt.Graphics;
import jetbrains.mps.nodeEditor.cells.ParentSettings;
import jetbrains.mps.nodeEditor.cells.EditorCell_Collection;
import jetbrains.mps.openapi.editor.TextBuilder;
import jetbrains.mps.editor.runtime.TextBuilderImpl;

public class ProgressBarCell extends AbstractCellProvider {
  private int width;
  private float percentage;
  private Color borderColor;
  private Color barColor;
  private int vpadding;
  public ProgressBarCell(SNode node, int width, float percentage, Color borderColor, Color barColor, int vpadding) {
    this(node, width, percentage, borderColor, barColor, vpadding, false);
  }
  public ProgressBarCell(SNode node, int width, float percentage, Color borderColor, Color barColor, int vpadding, boolean selectable) {
    super(node);
    this.width = width;
    this.percentage = percentage;
    this.borderColor = borderColor;
    this.barColor = barColor;
    this.vpadding = vpadding;
    this.selectable = selectable;
  }
  public EditorCell createEditorCell(EditorContext context) {
    EditorCell_Basic result = new EditorCell_Basic(context, getSNode()) {
      public void paintContent(Graphics g, ParentSettings parentSettings) {
        EditorCell_Collection parent = this.getParent();
        int x = getX();
        int y = parent.getY();

        int borderHeight = parent.getHeight() - 2 * vpadding;
        int barHeight = borderHeight - 1;

        int bx1 = x;
        int by1 = y + vpadding;

        g.setColor(borderColor);
        g.drawRect(bx1, by1, width, borderHeight);

        float f = percentage * width;
        int barWidth = ((int) f - 1);
        int bx2 = x + 1;
        int by2 = y + vpadding + 1;

        g.setColor(barColor);
        g.fillRect(bx2, by2, barWidth, barHeight);

      }
      public void relayoutImpl() {
        this.myWidth = width;
        this.myHeight = 10;
      }
      @Override
      public boolean isSelectable() {
        return false;
      }

      @Override
      public TextBuilder renderText() {
        return new TextBuilderImpl(percentage + "%");
      }
    };

    result.setSelectable(selectable);

    return result;
  }
  private boolean selectable;
}
